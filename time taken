#include<stdio.h>
#include<math.h>
int main()
{
  int i,s,u;
  float a,t;
  printf("enter the speed,acceleration,distance");
  scanf("%d%f%d",&u,&a,&s);
  for(i=1;i<=10;i++,s=s+3)
  {
      t=u+sqrt(u*u+2*a*s);
  }
  printf("At this floor time taken is %5f",t);
  return 0;
}
